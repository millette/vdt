{
  "name": "vdt",
  "version": "0.0.0",
  "license": "AGPL-3.0",
  "scripts": {
    "lint": "standard",
    "dev": "standard && next",
    "build": "standard && next build && next-build-id",
    "export": "next export",
    "start": "next start",
    "now666": "now out --static --public --name vdt",
    "clean": "rm -fr out .next",
    "all": "standard && rm -fr out .next && next build && next-build-id && next export"
  },
  "devDependencies": {
    "webpack": "^3.12.0",
    "next-build-id": "^2.0.1",
    "next-progressbar": "^1.0.0",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "@rematch/core": "1.0.0-alpha.9",
    "detect-node": "^2.0.3",
    "next": "^6.0.3",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-redux": "^5.0.7",
    "redux": "^4.0.0"
  }
}
